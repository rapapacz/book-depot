from FileSystem import *

filesystem = FileSystem()

FileSystem.write(self=filesystem, data="This text has been written to a file.")
FileSystem.append(self=filesystem, data="\nThis text has been appended to a file.")
FileSystem.read(self=filesystem)


